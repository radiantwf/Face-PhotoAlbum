<Window x:Class="MVVMDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:MVVMDemo.Views"
        Background="#FF2B2929" Foreground="WhiteSmoke">

    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border CornerRadius="5" BorderBrush="Gray" BorderThickness="2" Grid.Row="0">
            <Label Height="30" Width="400" Content="Multiple Tabs Dynamic Loading" Foreground="WhiteSmoke" />
        </Border>

        <Border CornerRadius="5" BorderBrush="Gray" BorderThickness="2" Grid.Row="1" Margin="0,5,0,0">
            <views:InventorySummaryView/>
        </Border>

    </Grid>
</Window>